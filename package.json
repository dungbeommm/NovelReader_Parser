{
  "name": "novelreader-parser-sources",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "devDependencies": {
    "playwright": "^1.46.0"
  },
  "scripts": {
    "check:syntax": "find sources -name '*.js' -print0 | xargs -0 -n1 node --check",
    "check:live": "node scripts/live_health_check.mjs"
  }
}
